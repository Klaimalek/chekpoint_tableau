ALGORITHM somme de deux tableaux
VAR
    T1, T2, T3: Array of float[];
    k,i,j,n,m,w: INTEGER;
    sum: float;
    existe: BOOLEAN;
BEGIN
    
    for i from 1 to n do
      for j  from 1 to m do 
       if T1[i]<> T1[j] & existe= false then
         T3[z]:=T1[i];
         existe := true;
        end if
     end for
    end for
     sum:=0;
     for k from 1 to w do
       sum:= sum+T3[z];
        write("les elts communs sont:" T3[z], "la somme est", sum);
END